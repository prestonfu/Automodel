# To run this recipe, please use the following command. Replace --nproc-per-node=8 with the number of GPUs on your node:
# torchrun --nproc-per-node=8 examples/llm_pretrain/pretrain.py --config examples/llm_pretrain/nemotron_nano_v3_pretrain.yaml


checkpoint:
  enabled: false
  checkpoint_dir: /mnt/checkpoints/
  model_save_format: safetensors
  save_consolidated: false
  load_base_model: false  # random init, no pretrained weights

benchmark:
  warmup_steps: 5
  peak_tflops: 989  # H100: 989, A100: 312
  nsys_start: -1
  nsys_end: -1
  nsys_ranks: []
  num_nodes: 1

step_scheduler:
  global_batch_size: 32
  local_batch_size: 16
  ckpt_every_steps: 1000
  val_every_steps: 250
  num_epochs: 1
  max_steps: 9500

dist_env:
  backend: nccl
  timeout_minutes: 1

rng:
  _target_: nemo_automodel.components.training.rng.StatefulRNG
  seed: 1111
  ranked: true

autopipeline:
  _target_: nemo_automodel.components.distributed.pipelining.autopipeline.AutoPipeline
  pp_schedule: 1f1b
  pp_microbatch_size: 2
  scale_grads_in_schedule: false

parallelizer:
  _target_: nemo_automodel.components.moe.parallelizer.parallelize_model
  activation_checkpointing: false

# Model: load architecture from Nemotron config, initialize with random weights
model:
  _target_: nemo_automodel.NeMoAutoModelForCausalLM.from_config
  trust_remote_code: true
  config:
    _target_: transformers.AutoConfig.from_pretrained
    pretrained_model_name_or_path: nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16
    trust_remote_code: true

compile:
  enabled: false
  mode: "default"
  fullgraph: false
  dynamic: true
  backend: null

distributed:
  _target_: nemo_automodel.components.distributed.fsdp2.FSDP2Manager
  dp_size: 1
  dp_replicate_size: 1
  tp_size: 1
  cp_size: 1
  ep_size: 1
  pp_size: 8
  sequence_parallel: false

loss_fn:
  _target_: nemo_automodel.components.loss.masked_ce.MaskedCrossEntropy

# Dataset: Megatron-Core format (preprocessed from nvidia/Nemotron-Pretraining-Dataset-sample)
# Preprocess with: tools/preprocess_megatron_dataset.py
dataset:
  _target_: nemo_automodel.components.datasets.llm.megatron_dataset.MegatronPretraining
  paths: /mnt/pretrain_data/megatron_nemotron/processed_data_*_text_document*
  index_mapping_dir: /mnt/pretrain_data/megatron_nemotron/mapping_dir
  tokenizer:
    _target_: nemo_automodel._transformers.auto_tokenizer.NeMoAutoTokenizer.from_pretrained
    pretrained_model_name_or_path: nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16
    trust_remote_code: true
  seq_length: 2048
  split: "1.0, 0.0, 0.0"
  splits_to_build: "train"

packed_sequence:
  packed_sequence_size: 0

dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  collate_fn: torch.utils.data.default_collate
  dataloader_type: "single"

validation_dataset:
  _target_: nemo_automodel.components.datasets.llm.megatron_dataset.MegatronPretraining
  paths: /mnt/pretrain_data/megatron_nemotron/processed_data_val_*_text_document*
  index_mapping_dir: /mnt/pretrain_data/megatron_nemotron/mapping_dir
  tokenizer:
    _target_: nemo_automodel._transformers.auto_tokenizer.NeMoAutoTokenizer.from_pretrained
    pretrained_model_name_or_path: nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-BF16
    trust_remote_code: true
  seq_length: 2048
  split: "1.0, 0.0, 0.0"
  splits_to_build: "train"
  num_val_samples: 256

validation_dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  collate_fn: torch.utils.data.default_collate
  dataloader_type: "single"

optimizer:
  _target_: torch.optim.AdamW
  lr: 6.0e-4
  betas: [0.9, 0.95]
  weight_decay: 0.1

lr_scheduler:
  lr_decay_style: cosine
  lr_warmup_steps: 10
  min_lr: 0.0

wandb:
  wandb_online: true
  project: nemotron_nano_v3_pretrain
  entity: prestonfu
  name: nemotron_nano_v3_pretrain
#   save_dir: <your_wandb_save_dir>
